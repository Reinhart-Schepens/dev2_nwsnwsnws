<% if( news.length === 0 ) { %>
<p>Er is geen nieuws beschikbaar</p>
<% } else { %> <% news.forEach((item) => { %>
<li><a href="/details/<%= item.slug %>"><%= item.title %></a></li>
<% }) %> <% } %>
<a href="./comments">Comments</a>
